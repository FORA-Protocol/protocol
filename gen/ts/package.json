{
  "name": "@fora-protocol/sdk-types",
  "version": "1.0.3",
  "private": true,
  "description": "Generated TypeScript types export: Zod schemas (./wire/schemas) + vocabulary constants (./vocab/*), generated from the proto via JSON Schema. Not published on its own: it ships inside @fora-protocol/sdk (built by sdk/ts/scripts/build.mjs), which exports these subpaths unchanged.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    "./wire/schemas": "./wire/schemas.ts",
    "./wire/base": "./wire/base.ts",
    "./wire/names": "./wire/names.ts",
    "./vocab/*": "./vocab/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && vitest run"
  },
  "peerDependencies": {
    "zod": "^3.23.0 || ^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^2.1.0",
    "zod": "^3.23.0"
  }
}
